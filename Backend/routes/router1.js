var express = require('express');
const {specs_rt} = require("../controller/funzioni");
const http = require("http");
const WebSocket = require("ws");
var router = express.Router();

// GET home page
//router.get('/api/specs_rt',specs_rt)

module.exports = router;